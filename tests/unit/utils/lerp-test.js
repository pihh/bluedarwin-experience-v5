import lerp from 'bluedarwin-experience-v5/utils/lerp';
import { module, test } from 'qunit';

module('Unit | Utility | lerp', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = lerp();
    assert.ok(result);
  });
});
