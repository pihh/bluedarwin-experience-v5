import wait from 'bluedarwin-experience-v5/utils/wait';
import { module, test } from 'qunit';

module('Unit | Utility | wait', function () {
  // TODO: Replace this with your real tests.
  test('it works', function (assert) {
    let result = wait();
    assert.ok(result);
  });
});
